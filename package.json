{
  "name": "contact-app",
  "version": "1.0.0",
  "description": "A simple Command Line Interface (CLI) application built with **Node.js** to store contact information (name, email, and phone number) in a JSON file.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "validator": "^13.5.2",
    "yargs": "^16.2.0"
  }
}
